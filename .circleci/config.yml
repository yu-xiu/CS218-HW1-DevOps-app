version: 2.1
jobs:
  run_tests:
    docker:
      - image: circleci/python:3.9
    steps:
      - checkout
      - run:
          name: Install Python dependencies
          command: /Users/yuxiu/anaconda3/envs/notes-app-env/bin/pip install -r requirements.txt
      - run:
          name: Run Pytest
          command: pytest
      - store_test_results:
          path: test-reports
      - store_artifacts:
          path: test-reports
workflows:
  build_test:
    jobs:
      - run_tests
